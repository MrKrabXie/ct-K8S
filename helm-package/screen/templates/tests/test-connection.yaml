apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "screen.fullname" . }}-test-connection"
  labels:
    {{- include "screen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "screen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
