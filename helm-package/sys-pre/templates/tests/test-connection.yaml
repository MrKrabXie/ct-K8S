apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sys-pre.fullname" . }}-test-connection"
  labels:
    {{- include "sys-pre.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sys-pre.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
