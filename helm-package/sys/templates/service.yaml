apiVersion: v1
kind: Service
metadata:
  name: {{ include "sys.fullname" . }}
  labels:
    {{- include "sys.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sys.selectorLabels" . | nindent 4 }}
