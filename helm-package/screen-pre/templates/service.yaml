apiVersion: v1
kind: Service
metadata:
  name: {{ include "screen-pre.fullname" . }}
  labels:
    {{- include "screen-pre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "screen-pre.selectorLabels" . | nindent 4 }}
