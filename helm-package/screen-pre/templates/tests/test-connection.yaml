apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "screen-pre.fullname" . }}-test-connection"
  labels:
    {{- include "screen-pre.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "screen-pre.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
